---
- hosts: linux
  gather_facts: no

  tasks:

  - name: "Install required software"
    apt:
      name: "{{ item }}"
      state: present
    become: yes
    with_items: [ 'httping', 'hping3', 'nmap' ]
